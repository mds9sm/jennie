fastapi==0.115.6
uvicorn[standard]==0.34.0
anthropic==0.52.0
asyncpg==0.30.0
pyyaml==6.0.2
rapidfuzz==3.11.0
python-dotenv==1.0.1
pydantic==2.10.4
sse-starlette==2.2.1
boto3>=1.35.0
requests>=2.31.0
redshift-connector>=2.1.0
python-multipart>=0.0.9
mcp>=1.0.0
python-docx>=1.1.0
duckdb>=1.1.0
# pgvector extension used via raw SQL (asyncpg) — no Python package needed.
# The postgres CREATE EXTENSION vector must be run on RDS separately.
# DOMO uses direct REST calls (no pydomo — avoids SSL issues in containers)

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
